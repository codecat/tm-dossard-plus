namespace Constants {
    const uint64 DOSSARD_OFFSET = 0x20C; // The numbers
    const uint64 TRIGRAM_OFFSET = DOSSARD_OFFSET + 0x2; // The letters
    const uint64 COLOR_OFFSET = 0x214;
    const string HOOK_PATTERN = "f3 0f 10 52 ?? 4c 8b da f3 0f 59 15 ?? ?? ?? ?? 4c 8b d1";
}